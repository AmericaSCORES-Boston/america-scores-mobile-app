import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 75,
  },
  textCenter: {
    alignSelf: 'center'
  },
  loginButton: {
    marginTop: 30
  }
});

export default styles;